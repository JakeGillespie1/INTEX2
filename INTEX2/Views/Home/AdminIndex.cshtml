@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@using Microsoft.AspNetCore.Identity
@using System.Security.Principal
@inject SignInManager<AppUser> SignInManager
@inject UserManager<AppUser> UserManager

@{
    ViewData["Title"] = "Home Page";
}

@{
    string userRole = "Default"; // Default role

    if (User.Identity.IsAuthenticated)
    {
        if (User.IsInRole("Admin"))
        {
            userRole = "Admin"; // Set role to Admin if user is an admin
        }
        else if (User.IsInRole("Member") || User.IsInRole("User"))
        {
            userRole = "User"; // Set role to User if user is a member or user
        }
    }
}

<vc:nav-bar-types user="Admin"></vc:nav-bar-types>




<div class="admin-image-cont">
    <div class="admin-image-wrapper">
        <img src="~/img/driver.jpg" id="admin-pic" alt="3 peeps" />
       
        <a asp-area="" asp-controller="Home" asp-action="ProductsPage">WELCOME ADMIN</a>
    </div>
</div>

<div class="admin-buttons">
    
    <a href="#" class="admin-button">Review Order</a> 
    <a asp-area="" asp-controller="Home" asp-action="ProductsList" class="admin-button">Edit Product</a>
    <a asp-area="" asp-controller="Home" asp-action="Customers" class="admin-button">User List</a>
</div>
<vc:footer-types></vc:footer-types>